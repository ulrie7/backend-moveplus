module.exports = async() => {
    console.log(`
  Usage: orangcode <command>:<subCommand> [options]
  
  Commands:
    db:seed <module>       Seed a specific module
    help                   Show this help message
    `);
};